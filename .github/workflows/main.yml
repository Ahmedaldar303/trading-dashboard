name: React Dashboard
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: npm install
      - run: npm run build
        env:
          # في React، يجب أن يبدأ اسم المتغير بـ REACT_APP_
          REACT_APP_API_KEY: ${{ S1oEHj8hiijWeQIKG3CEVFcVPt8BpCarDLXxFy9p }}
